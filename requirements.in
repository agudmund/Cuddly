# -Cuddly - requirements.in pip dependency set
# -The last of the cuddly package list held its two dependencies close and would not let go, For Enjoying
# -Built using a single shared braincell by Yours Truly and various Intelligences
# No version pins; Python is the floor, pip resolves the rest.

PySide6                 # Qt bindings
numpy                   # array math
